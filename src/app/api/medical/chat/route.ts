import { NextRequest, NextResponse } from "next/server";
import { generateText, generateWithImage } from "@/lib/ai/client";

export async function POST(req: NextRequest) {
   try {
      const { message, history, imageUrl } = await req.json();

      // 1. Red Flag Detection (Dermatology Emergencies)
      const redFlags = [
         "호흡곤란", "숨이 차", "기도 폐쇄", "의식 저하", "기절", "실신",
         "심한 알레르기", "아나필락시스", "온몸이 부어", "눈이 안 떠져",
         "고열", "39도", "심한 통증", "화상", "물집이 전신"
      ];

      const isRedFlag = redFlags.some(flag => message.includes(flag));

      if (isRedFlag) {
         return NextResponse.json({
            role: "ai",
            content: "🚨 [응급 알림] \n지금 말씀하신 증상은 응급 상황(아나필락시스, 심한 감염/화상 등)일 가능성이 있습니다. \n\n즉시 119에 연락하거나 가까운 응급실/피부과 전문 병원을 방문해 주세요."
         });
         // Calculate turn count based on history (User + AI = 1 turn, but history is array of messages)
         // history length 0 -> Turn 1
         // history length 2 -> Turn 2
         // history length 4 -> Turn 3
         const currentTurn = Math.floor(history.length / 2) + 1;

         let turnInstruction = "";
         let charLimit = "공백 포함 최대 200자";

         if ([3, 5, 7, 10].includes(currentTurn)) {
            charLimit = "공백 포함 최대 400자";
            turnInstruction = `
         [현재 대화 턴: ${currentTurn}번째]
** 중요 지침 **:
         1. 이번 턴에서는 사용자의 고민과 관련된 ** 피부과 미용 시술(스킨부스터, 리프팅, 레이저 등) ** 에 대해 ** 상세하게 ** 설명해야 한다.
2. 특히 해당 시술의 ** 역할, 장점, 그리고 기대 효과 ** 를 구체적으로 기술하라.
3. 특정 상품명보다는 '스킨부스터 계열', 'HIFU 리프팅' 처럼 범주 위주로 언급하고, "피부과 전문의와 상의해보라"는 권유를 포함해라.
4. ** [RESERVATION_TRIGGER] ** 태그를 답변 맨 마지막에 반드시 포함시켜라. (이 태그는 사용자에게 보이지 않고 예약 모달을 띄우는 용도임)
            `;
         }

         const textSystemPrompt = `
너는 한국의 피부과 환경을 이해하고 있는 "피부 미용 시술 상담용 AI 컨시어지"이다.

[필수 제약 사항]
         1. ** 출력 길이 **: ${charLimit} 이내로 작성할 것.
2. ** 형식 금지 **: "(공감)", "(분석)", "(질문)" 같은 ** 라벨 / 괄호 표기를 절대 하지 말 것.** 자연스러운 줄글로 이어갈 것.
3. ** 답변 구조 **:
         (1) ** 공감 **: 사용자의 고민에 대해 깊이 공감하고 안심시키는 따뜻한 멘트로 시작.
   (2) ** 상세 분석 **: 사용자의 증상 / 고민에 대해 ** 전문적이고 세부적인 분석 ** 을 제공할 것.단순한 호응이 아니라, 피부 생리학적 원리나 상태를 구체적으로 설명해 줄 것.
   (3) ** 질문 **: 상담을 이어가기 위한 질문 1가지(필수).사용자 답변에 근거해서 질문할 것.

            ${turnInstruction}

         [역할 및 태도]
            - 친절하고 따뜻하며, 사용자의 걱정을 덜어주는 태도.
- 의료법 준수: 진단 / 처방 금지, "도움이 될 수 있다" 표현 사용.
- ** 시술 종류·장단점·고려 요소를 미리 이해하도록 도와주는 안내자 ** 역할.

[대화 내역]
${history.map((msg: any) => `${msg.role === 'user' ? '사용자' : 'AI'}: ${msg.content}`).join("\n")}
         사용자: ${message}
         AI:
         `;
         responseText = await generateText(textSystemPrompt, "medical");
      }

      return NextResponse.json({
         role: "ai",
         content: responseText.trim()
      });

   } catch (error) {
      console.error("Medical Chat API Error:", error);
      return NextResponse.json(
         { error: "Internal Server Error" },
         { status: 500 }
      );
   }
}
